# pj_manage_app

## Setup
```
npm init
npm install --save-dev lite-server

# package.json
 "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "lite-server"  # add
  },
  
tsc --init
npm start
tsc -w
```